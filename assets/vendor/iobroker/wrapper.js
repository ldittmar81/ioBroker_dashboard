import {
  AdminConnection,
  Connection,
  PROGRESS,
  ERRORS,
  PERMISSION_ERROR,
  NOT_CONNECTED
} from './index.js';

// Globale Variablen bereitstellen
window.AdminConnection   = AdminConnection;
window.Connection        = Connection;
window.PROGRESS          = PROGRESS;
window.ERRORS            = ERRORS;
window.PERMISSION_ERROR  = PERMISSION_ERROR;
window.NOT_CONNECTED     = NOT_CONNECTED;
